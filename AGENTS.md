# AGENTS.md

## 1. 项目定位

本仓库是“新能源企业经营分析智能平台（AI 增强 BI）”的独立正式开发仓库。

产品服务于新能源充电运营经营分析，核心能力包括经营指标语义层、经营驾驶舱、受控 ChatBI、Query Plan、确定性 SQL 编译、SQL 安全、异常诊断、贡献归因、结构化会话状态、报告草稿、评测与证据链。

不得把本项目实现成通用聊天机器人、上传 CSV 后直接调用大模型的演示程序、自由 SQL 工具或多 Agent 自动执行系统。

## 2. 当前阶段

项目负责人于 2026-07-22 将项目切换为**快速落地模式**，一次性授权连续执行 Phase 1—8。

- 按“实现 → 测试 → 修复 → 阶段验收 → 独立 Git 提交”连续推进；
- 每个阶段满足测试、证据、可回滚和真实性边界后自动进入下一阶段；
- 不得推翻 DEC-01—DEC-10、五类开发合同和已冻结的产品范围；
- 只有仓库外修改、真实数据/密钥、不可逆迁移、安全/对账/固定评测三轮失败或真实运行时硬阻塞才暂停。

### 2.1 P5C 项目所有者规则增补（2026-08-08）

本轮只执行 P5C“事实基线、安全和发布真相收敛”，不得提前开发 RAG、Memory、SQLBot/NL2SQL 或 P6 业务功能。P5C 通过后，后续阶段才可从同一冻结 SHA 建立互斥 worktree 并行实施。

后续 4.1 落地目标遵循以下新增约束；其中涉及 P0 冻结范围变化的事项，必须先完成 ADR/DoR 和合同更新，不能用计划文档静默覆盖已发布范围：

1. 最多 7 个自然日内形成可部署、可启动、可操作、可自动验收、可追溯和可回滚的本地/服务器候选；真实生产准入继续独立判断。
2. 业务事实数据必须来自已批准的可追溯公开数据快照，或来自已批准业务规则的确定性衍生；不得随机生成订单、会话、收入、成本、客户或设备事实行。当前 P5B 的固定种子模拟数据在 DATA 阶段完成新数据落库、质量、发布和回滚验收前不得更名或冒充公开真实数据。
3. 所有前端业务数据必须先写入数据库，再经正式后端 API、权限过滤和已发布语义层提供；禁止前端硬编码、静态业务 JSON、随机数和接口失败后造数。
4. 数据性质、时间范围、来源和 `run_id` 继续由统一且持续可见的全局状态区承载；普通业务组件不重复堆叠工程标签。后台数据目录和审计必须保留发布者、许可证、原始哈希、导入批次、变换版本和数据集版本。
5. 不得将公开数据描述为某一家真实企业的生产经营数据、真实客户验收结果或真实经营收益。
6. SQLBot 实时路由、Canary 和受治理的开放式 NL2SQL 是后续 4.1 阶段目标；核心 15 指标和高风险问题继续优先走确定性引擎，所有候选 SQL 必须进入统一 SQL Policy Gateway、权限校验、Query Guard 和只读执行。
7. 受治理的开放式 NL2SQL 只能访问已注册、已授权的数据源、Schema、表和列；禁止 DDL/DML、多语句、系统表、跨库、敏感列和无界查询。它不等于 P0 排除的任意数据库自由 SQL。
8. 每阶段结束必须原位更新根目录唯一一键启动文件，并从完全停止状态冷启动验证新增能力；未通过不得判定阶段 PASS。
9. RAG 自动治理只允许受控系统身份按预定义清单执行，并记录 actor 类型、reason code、before/after 和 audit id；不得伪装为真实员工人工审批。
10. 记忆与遗忘的后续实现必须包含持续运行的 Worker、Outbox、幂等、重试、跨存储删除和删除后不可召回验证，并继续受 `docs/memory_contract_v0.1.md` 约束。
11. 项目所有者允许在 P5C 通过后进入 P6 本地开发；预警、报告和指标治理必须同时具备实体、迁移、API、UI、权限、审计和 E2E。P6 本地通过不等于生产准入或跨系统自动执行获批。
12. 企业外部资源缺失时可使用契约、沙箱、故障注入和自动化替代验证推进本地交付；正式生产门禁必须单列，不得由替代验证冒充关闭。
13. 禁止微服务全面拆分、全面前端重写、多 Agent 自主写库和与本期验收无关的扩展。
14. 每个任务必须输出工作树、分支、开始/结束 HEAD、修改文件、测试退出码、证据、一键启动验证、风险、回滚和唯一下一行动。

## 3. 冻结决策

- 独立 Git 仓库、独立业务域、独立数据库、独立 README、独立演示和独立证据；
- Alpha 使用固定随机种子和业务规则驱动的模拟数据，时间范围为 2025-01-01 至 2026-06-30；
- P0 核心用户为经营负责人、区域运营经理、数据分析师/系统管理员；
- P0 核心指标固定为 `docs/metric_dictionary_v0.1.md` 中的 15 项；
- P0 不开放自由 SQL 或管理员实验入口；
- 核心查询链路固定为：自然语言问题 → Query Plan → 确定性参数化 SQL 编译 → Query Guard → 只读执行 → 结构化结果 → Answer Guard；
- 本项目只生成可审核报告草稿；自动邮件、工单和跨系统执行属于项目三；
- Alpha 周期从 DoR 通过且 Phase 1 获批之日 T0 起算，目标 T0+30 个日历日；
- P0 记忆只包含工作状态、同一会话结构化上下文、明确覆盖、新会话隔离和 `analysis_run`；长期偏好、历史案例、纠错记忆和自动学习属于 P1；
- 组织规则只能由管理员或指标负责人审批发布；
- 记忆 TTL、删除和审计规则以 `docs/memory_contract_v0.1.md` 为准。

## 4. 事实优先级

1. 当前运行、迁移和测试证据；
2. 已发布数据库 Schema 与指标版本；
3. `docs/00_PROJECT_FACT_BASELINE.md`；
4. 已批准 ADR 与 `docs/decision_log.md`；
5. 已批准的合同和产品/技术文档；
6. 代码注释和历史说明。

发生冲突时必须报告，不得静默选择。

## 5. 真实性规则

必须区分：已设计、已实现、已测试、已验证、模拟数据、规划中。

当前不得宣称：

- 已在企业生产环境上线；
- 已有真实客户使用；
- 已产生真实经营收益；
- 已支撑真实大规模场站；
- 模拟数据是企业真实数据。

所有页面、报告、导出和 README 必须显示“模拟数据”，并展示数据时间、来源和 `run_id` 位置。

### 5.1 前端数据呈现与数据链路

- 前端业务页面应聚焦经营分析内容，数据性质、时间范围、来源和 `run_id` 由统一且持续可见的全局状态区承载，不在每个业务组件中重复堆叠说明；
- 不得将模拟数据标注或暗示为真实生产数据，不得通过隐藏数据性质、伪造来源或使用“真实数据”等未经验证的描述使用户产生误解；
- 前端不得硬编码业务结果、随机生成业务数据、直接读取本地 Mock/fixture，或在接口失败时使用虚构数据静默兜底；
- 前端所需数据缺失时，可以基于已批准的真实业务规则、指标口径和固定随机种子生成模拟数据，但必须先通过可重复执行的数据初始化脚本、种子任务或迁移写入数据库；
- 所有前端业务数据必须经正式后端 API、权限过滤和已发布语义层读取；不得绕过数据库与后端直接拼装展示；
- 模拟数据生成规则、时间范围、版本、随机种子和 `run_id` 必须可追溯，且不得混入真实客户、个人、密钥或生产连接信息；
- 新增前端数据链路必须提供数据库落库证据、API 响应证据和前端 E2E 验证，证明页面展示值来自数据库而非前端构造。

## 6. 工作区与安全规则

- 只修改当前仓库；
- 不得写入 C 盘；
- 不得删除文件，除非任务明确授权；
- 不得执行不可逆数据库操作；
- 不得安装全局依赖；
- 不得提交 `.env`、密钥、密码、Token 或真实连接串；
- 测试必须使用隔离数据库或临时目录；
- 修改前检查 Git 状态；
- 每个工作包独立提交；
- 数据库迁移必须可回滚；
- 不得将其他项目的代码、数据、迁移或证据直接耦合进本仓库。

## 7. 架构规则

- 使用模块化单体，不拆微服务；
- 核心指标必须来自已发布指标语义层；
- LLM 不得直接生成和执行核心 SQL；
- LLM 不得生成结构化结果中不存在的业务数字；
- 当前数据库事实和本轮明确指令高于历史记忆；
- 权限过滤必须在数据查询和记忆召回前执行；
- Query Guard 的拒绝不可绕过；
- 报告只引用已验证结构化结果；
- 设备与经营变化默认只能表述为相关或疑似影响，不得无证据宣称因果。

## 8. 修改与测试要求

每次修改必须说明目标、范围、非目标、修改文件、数据库影响、安全影响、测试、验收证据、限制和回滚方式。

按修改范围运行相关的单元、契约、集成、数据质量、安全负向、AI 固定评测和 E2E 测试。不得删除、跳过或弱化失败测试来制造通过结果。

不得创建只有占位文本却被标记为完成的业务功能。

## 9. 任务输出要求

每次任务结束必须输出：

1. 完成内容；
2. 修改文件；
3. 执行命令；
4. 测试结果；
5. 验收证据；
6. 未完成事项；
7. 风险；
8. 回滚方式；
9. 是否允许进入下一阶段。
