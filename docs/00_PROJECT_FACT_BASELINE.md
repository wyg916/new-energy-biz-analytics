# 项目事实基线

> 事实日期：2026-07-22
> 当前阶段：Phase 0.1 开发输入补齐与 DoR 复审
> 数据标签：模拟数据
> 本文件是当前项目状态声明；任何 README、演示和简历描述不得超出本文件证据。

## 已设计并已批准

- 产品定位、P0/P1/P2 范围和排除项；
- DEC-01—DEC-10；
- P0 三类核心用户；
- P0 15 项指标清单与基础公式；
- 模拟数据范围、规模、固定 seed 和场景要求；
- 模块化单体、受控 Query Plan、确定性 SQL 编译和 Query Guard 路径；
- P0 结构化会话状态与 P1 长期记忆边界；
- 报告草稿与运营 Agent 边界；
- Alpha 从 T0 起 30 个日历日的目标窗口。

## 已形成的开发前合同

- 数据合同 v0.1；
- 15 项指标字典 v0.1；
- Query Plan 合同 v0.1；
- RBAC 与 SQL 安全合同 v0.1；
- P0 记忆与 `analysis_run` 合同 v0.1；
- 40 道 ChatBI 固定评测题 v0.1；
- 低保真静态交互原型；
- 阶段计划、缺失输入和 DoR 复审。

上述产物均为“已设计/已批准的开发输入”，不表示代码已经实现或测试通过。

## 尚未实现

- FastAPI 后端、React 前端和正式页面；
- PostgreSQL Schema、pgvector、Redis、Celery 和 Alembic 迁移；
- 模拟数据生成器、实际模拟数据批次和数据质量运行；
- 指标注册表、指标服务和驾驶舱 API；
- Query Planner、SQL Compiler、Query Guard 和只读执行器；
- Answer Guard、RAG、ChatBI 结果和证据面板；
- 会话状态、`analysis_run` 和审计存储；
- 异常、收入/毛利拆解和贡献归因；
- 报告草稿与导出；
- Docker Compose、自动化测试、固定评测执行和 Alpha 验收。

## 尚未测试或验证

- 所有正式业务能力；
- 指标 SQL 在真实 PostgreSQL Schema 上的正确性；
- 模拟数据的规模、分布和汇总一致性；
- SQL Guard 的危险请求拒绝率；
- 固定评测集准确率；
- 原型的浏览器视觉与可访问性；
- 全新环境安装、迁移、回滚和部署。

## 数据真实性

- 当前没有企业生产数据；
- Alpha 计划使用 2025-01-01 至 2026-06-30 的规则生成模拟数据；
- 计划规模不等于已生成规模；
- 页面、报告、导出和 README 必须标记“模拟数据”；
- 不得宣称生产上线、真实客户使用、真实收入提升或真实大规模场站支撑。

## 当前 Git 基线

- 独立 Git 仓库；
- 默认分支 `main`；
- Phase 0.1 完成后建立首次基线提交；
- 未配置远程仓库，不进行推送。

## 允许的状态标签

- 已设计；
- 已批准；
- 已实现；
- 已测试；
- 已验证；
- 模拟数据；
- 规划中。

如果缺少运行或测试证据，只能使用“已设计/已批准/规划中”，不能升级为“已实现/已测试/已验证”。
