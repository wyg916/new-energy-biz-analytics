# ADR-001：平台核心保持模块化单体

状态：Accepted（2026-07-30）

## 背景

现有系统已经是一个可部署单元。P0 需要解耦平台与 charging_ops，而不是引入分布式复杂度。

## 决策

平台核心、场景包和 API 继续在模块化单体内，通过版本化 Python/数据合同隔离依赖。只有获得独立伸缩、故障隔离或组织所有权证据后才评估拆服务。

## 后果

部署、事务和回滚保持简单；必须用导入规则和合同测试防止场景反向污染平台层。

