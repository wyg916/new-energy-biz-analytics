# ADR-003：核心查询保持确定性编译

状态：Accepted（2026-07-30）

## 决策

核心链路固定为自然语言问题 → Query Plan → 已发布语义校验 → 确定性参数化 SQL → Query Guard → 只读执行 → 结构化结果 → Answer Guard。

## 理由

经营数字需要稳定口径、权限前置、可回归和证据链。LLM 直接生成并执行 SQL 无法满足该边界。

## 后果

未来 SQLBot 只能作为受控 Planner 或非核心探索适配器，且必须可审计、可关闭、不能绕过 Guard。

