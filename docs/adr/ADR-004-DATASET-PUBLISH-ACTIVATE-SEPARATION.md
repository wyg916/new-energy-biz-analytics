# ADR-004：数据集发布与激活分离

状态：Accepted（2026-07-30）

## 决策

发布只创建不可变 DatasetVersion；激活通过单独的 ActivationPointer 原子切换。正式消费者不得自行选择“最新版本”。

## 理由

当前快照发布与部分消费者读取之间存在语义空隙。分离发布和激活才能安全审批、兼容性验证、并发控制与回滚。

## 后果

当前 `PublishedStationSnapshot` 被定义为过渡适配器，不能宣称全平台激活；P2 需新增可回滚迁移和消费者一致性测试。

