# ADR-005：权限过滤先于查询、召回和记忆

状态：Accepted（2026-07-30）

## 决策

服务端从可信身份上下文解析 tenant/org/workspace/role/scope，并在数据查询、知识召回和记忆加载前应用默认拒绝策略。

## 理由

事后过滤会泄露候选数据、提示词上下文或跨会话状态；应用层 UI 隐藏不是安全边界。

## 后果

Provider 接口必须接收 IdentityContext；缓存键、审计和测试都包含作用域。当前单客户 RBAC 只能标记为过渡实现。

