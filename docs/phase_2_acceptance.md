# Phase 2 验收记录

状态：已完成。数据均为固定种子 `20260722` 生成的模拟数据，不含真实企业或个人信息。

## 实现范围

- 3 区域、6 城市、30 场站、120 设备、10,000 模拟用户、2025-01-01 至 2026-06-30 连续日期模型。
- 默认约 300,000 会话，以及场站日电费成本、可变费用、设备日状态区间。
- 15 项冻结指标语义目录和统一计算服务；除零返回 `NULL`。
- 关键数据质量规则、来源标签和可发布批次记录。
- 第二版可逆 Alembic 迁移。

## 验收与回滚

- 数据/指标测试夹具实际生成完整 546 天、3/6/30/120/10,000 维度和 1,000 条会话；20/20 条 DQ 规则通过。
- 15/15 指标计算、派生公式对账、比率范围及除零返回 `NULL` 通过。
- 全后端测试 8/8 通过；阶段测试运行覆盖率 93%。
- Alembic `0001 → 0002 → 0001 → 0002` 往返通过。

默认生成命令为 `python -m app.data.seed --session-count 300000`；完整 300,000 会话冷环境生成与容器数据库证据在 Phase 8 验收执行。回滚顺序为 `alembic downgrade 0001`，再回滚本阶段 Git 提交。
