# Phase 4 验收记录

状态：已完成。

## 实现范围

- 版本化 Pydantic Query Plan Schema 与语义校验。
- 中文指标同义词、时间、区域/场站、意图与澄清的确定性解析。
- 仅从批准指标模板生成参数化 SQL；授权场站由服务端注入并与请求范围取交集。
- SQLGlot AST Guard、单 SELECT、表白名单、无注释、多语句拒绝、参数与硬行数限制。
- 只读执行器只接受内部 `CompiledQuery`；产品不存在外部 SQL 执行入口。

## 回滚

本阶段无迁移；回滚独立 Git 提交即可。

## 运行验收证据

- 安全与查询专项测试 15/15 通过。
- 12/12 组 DDL、DML、多语句、注释、系统目录、文件函数、延时函数及 `COPY PROGRAM` 攻击均被拒绝，拒绝率 100%。
- 受控 Query Plan 实际编译并在模拟数据库只读执行；SQL 中不含自然语言区域名或授权 ID 字面量。
- 区域请求与授权范围空交集时 fail-closed。
