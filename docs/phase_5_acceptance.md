# Phase 5 验收记录

状态：已完成。

## 实现范围

- 自然语言问题到 Query Plan、权限解析、编译、Guard、只读执行、结构化结果和 Answer Guard 的完整链路。
- 指标查询、趋势、排名、比较、澄清和安全拒绝响应。
- 图表数据和证据面板包含模拟数据、时间、来源、批次、run、计划/SQL hash、指标版本与 Guard 状态。
- 普通经营角色不显示 SQL；分析师/管理员可在受控证据响应查看编译 SQL。
- 回答文本只格式化结构化结果中的数字；解释模式明确为 deterministic，不伪装外部模型能力。

## 回滚

本阶段无迁移；回滚独立 Git 提交，保留 Phase 4 的结构化查询能力。

## 运行验收证据

- 固定评测集 40/40 通过，通过率 100%；报告位于 `tests/evaluation/output/chatbi_eval_v0.1.*`。
- ChatBI 完整链路与越权专项测试 2/2 通过；澄清、危险意图拒绝、证据字段和 Answer Guard 均有断言。
- 前端真实性格式测试 3/3 通过；TypeScript 与 Vite 生产构建通过。
- 回答中的业务数字全部由结构化结果格式化，无依据业务数字计数为 0；未硬编码评测答案。

固定评测报告验证 Query Plan 子集、澄清/拒绝和安全行为；经营数字正确性由 Phase 2 指标对账及本阶段数据库集成测试覆盖，两类证据不混为一谈。
