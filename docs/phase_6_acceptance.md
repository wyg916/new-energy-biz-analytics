# Phase 6 验收记录

状态：已完成。

## 实现范围

- 用户/会话隔离的结构化 `session_state`，仅保存指标、维度、过滤、时间、比较、来源 run 和版本，不保存全文历史。
- 同会话继承、明确覆盖、`state_version` 递增、新会话隔离、清空和最长 7 天 TTL。
- `analysis_run` 保存计划、身份范围、版本、SQL/result/answer digest、状态、耗时与数据批次，保留策略为 180 天。
- 长期偏好、历史案例召回和自动学习仍明确不实现。
- 工作阶段以运行状态和审计事件表达；大结果不写入会话状态。

## 回滚

先执行 `alembic downgrade 0002` 删除本阶段两张治理表，再回滚独立 Git 提交。

## 运行验收证据

- 记忆专项测试 3/3 通过：继承、区域覆盖、版本递增、新会话隔离、跨用户 403、清空、过期排除、运行证据 digest 和工作状态释放。
- Alembic `0002 → 0003 → 0002 → 0003` 往返通过。
- 前端测试 3/3 及生产构建通过；问数页保留本会话 ID，并允许显式新建隔离会话。
- 运行完成后工作记忆立即释放；会话只保存结构化状态，未新增任何长期偏好或历史案例表。
