# P5 企业告警联调

状态：本地适配器沿用 P4 PASS；企业联调 `CONDITIONAL`。

用户未提供明确授权的企业预生产接收端、签名身份或网络白名单，因此本轮没有发送外部消息。未经授权外部调用数为 0。

关闭门禁时必须以非真实客户、非完整业务明细的合成事件验证认证/签名、重试、幂等、熔断、超时、脱敏、限流、失败审计和恢复通知；payload 与日志中不得包含 Secret。必须记录接收端、run_id、请求/响应摘要哈希和 Owner，不能用本地适配器替代企业系统证据。
