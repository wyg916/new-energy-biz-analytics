# Phase 0.1 低保真原型

本目录仅用于开发前页面范围、状态和证据位置评审，不是正式前端，也不连接后端。

打开 `index.html` 可切换：

- 登录；
- 经营驾驶舱；
- 智能问数；
- 证据面板；
- 异常中心；
- 指标中心；
- 分析记录；
- 正常、空、错误、数据延迟和无权限状态。

所有展示数字均为静态模拟示例，数据截至 2026-06-30，来源为“规则生成模拟数据”。正式实现必须依据版本化 API 和结构化结果重新开发，不能复制本原型中的静态数值作为业务逻辑。
